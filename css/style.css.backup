/* TrendSpotter Daily - Viral Content Design */
:root {
  --primary-color: #FF6B35; /* Vibrant orange-red for excitement */
  --secondary-color: #F7931E; /* Bright orange for energy */
  --accent-color: #00A8CC; /* Blue for trust */
  --background-color: #ffffff;
  --text-color: #212121;
  --text-light: #757575;
  --border-color: #e0e0e0;
  --card-bg: #ffffff;
  --shadow: rgba(0, 0, 0, 0.15);
  --highlight: #fff3cd; /* Light yellow for highlights */
  --viral-red: #e53935; /* Attention grabbing red */
  --trending-purple: #8e44ad; /* Purple for trending */
  --success-green: #27ae60; /* Green for positive */
  --gradient-start: #ff9a9e;
  --gradient-end: #fecfef;
  --card-shadow: 0 10px 30px rgba(0,0,0,0.1);
  --button-gradient: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  line-height: 1.7;
  color: var(--text-color);
  background-color: var(--background-color);
  position: relative;
  background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
}

/* Animated background for extra attention */
body::before {
  content: "";
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: linear-gradient(45deg, var(--gradient-start), #fad0c4, var(--accent-color), #c2e9fb);
  background-size: 400% 400%;
  animation: gradientBG 15s ease infinite;
  z-index: -1;
  opacity: 0.05;
}

@keyframes gradientBG {
  0% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
  100% { background-position: 0% 50%; }
}

.container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 20px;
}

header {
  background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
  color: white;
  padding: 1.5rem 0;
  border-bottom: 4px solid var(--accent-color);
  box-shadow: 0 4px 20px rgba(255, 107, 53, 0.3);
  position: sticky;
  top: 0;
  z-index: 1000;
  text-align: center;
}

header .container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
}

header h1 {
  font-size: 2.8rem;
  color: white;
  letter-spacing: -0.5px;
  font-weight: 800;
  margin: 0;
  text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.3);
  background: linear-gradient(to right, #ffffff, #ffecd2);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  font-family: 'Georgia', serif;
}

header p {
  color: #ffecd2;
  font-size: 1.2rem;
  font-weight: 500;
  margin: 0;
  text-shadow: 1px 1px 4px rgba(0, 0, 0, 0.3);
  max-width: 600px;
}

.viral-badge {
  background: linear-gradient(45deg, var(--viral-red), var(--trending-purple));
  color: white;
  padding: 0.4rem 1.2rem;
  border-radius: 30px;
  font-size: 1rem;
  font-weight: bold;
  text-transform: uppercase;
  letter-spacing: 1.5px;
  animation: pulse 2s infinite;
  margin-top: 0.5rem;
  box-shadow: 0 4px 10px rgba(142, 68, 173, 0.4);
}

@keyframes pulse {
  0% { transform: scale(1); box-shadow: 0 0 0 0 rgba(229, 57, 53, 0.7); }
  70% { transform: scale(1.05); box-shadow: 0 0 0 10px rgba(229, 57, 53, 0); }
  100% { transform: scale(1); box-shadow: 0 0 0 0 rgba(229, 57, 53, 0); }
}

nav {
  background-color: var(--card-bg);
  padding: 1.2rem 0;
  border-bottom: 2px solid var(--primary-color);
  box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
  position: sticky;
  top: 130px; /* Account for header */
  z-index: 999;
  backdrop-filter: blur(10px);
  background: rgba(255, 255, 255, 0.9);
}

nav ul {
  list-style: none;
  display: flex;
  justify-content: center;
  flex-wrap: wrap;
  gap: 0.8rem;
}

nav li a {
  text-decoration: none;
  color: var(--text-color);
  font-size: 1rem;
  font-weight: 700;
  padding: 0.8rem 1.5rem;
  border-radius: 30px;
  transition: all 0.3s ease;
  position: relative;
  overflow: hidden;
  border: 2px solid transparent;
  background: linear-gradient(to right, #f8f9fa, #ffffff);
  box-shadow: 0 4px 6px rgba(0,0,0,0.1);
}

nav li a:hover {
  background: var(--button-gradient);
  color: white;
  transform: translateY(-3px);
  box-shadow: 0 8px 15px rgba(255, 107, 53, 0.4);
}

main {
  padding: 3rem 0;
}

.trending-bar {
  background: linear-gradient(90deg, var(--viral-red), var(--trending-purple), var(--primary-color));
  color: white;
  padding: 1rem;
  text-align: center;
  font-weight: bold;
  font-size: 1.1rem;
  position: sticky;
  top: 190px; /* Account for header and nav */
  z-index: 998;
  animation: slideIn 0.5s ease-out;
  box-shadow: 0 4px 10px rgba(0,0,0,0.2);
}

@keyframes slideIn {
  from { transform: translateX(-100%); opacity: 0; }
  to { transform: translateX(0); opacity: 1; }
}

h1 {
  font-size: 2.6rem;
  margin-bottom: 1.5rem;
  color: var(--primary-color);
  font-weight: 800;
  line-height: 1.2;
  position: relative;
  padding-bottom: 1.2rem;
  text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.1);
  text-align: center;
}

h1::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 150px;
  height: 5px;
  background: var(--button-gradient);
  border-radius: 3px;
  box-shadow: 0 4px 10px rgba(255, 107, 53, 0.4);
}

h2 {
  font-size: 1.9rem;
  margin: 2.2rem 0 1.2rem;
  color: var(--trending-purple);
  font-weight: 700;
  position: relative;
  padding-bottom: 0.6rem;
}

h2::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 0;
  width: 90px;
  height: 4px;
  background: var(--accent-color);
  border-radius: 2px;
}

h3 {
  font-size: 1.5rem;
  margin: 1.7rem 0 0.85rem;
  color: var(--primary-color);
  font-weight: 700;
}

p {
  margin-bottom: 1.2rem;
  color: var(--text-color);
  font-size: 1.1rem;
  line-height: 1.8;
}

.date {
  color: var(--text-light);
  font-size: 0.95rem;
  margin-bottom: 1.2rem;
  display: block;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.8px;
  font-style: italic;
}

.category-tag {
  display: inline-block;
  background: var(--button-gradient);
  color: #fff;
  padding: 0.5rem 1.2rem;
  border-radius: 30px;
  font-size: 0.9rem;
  text-transform: uppercase;
  letter-spacing: 1.2px;
  font-weight: 700;
  margin-bottom: 1.2rem;
  box-shadow: 0 4px 8px rgba(255, 107, 53, 0.3);
  animation: fadeInUp 0.5s ease-out;
  transition: all 0.3s ease;
}

.category-tag:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 12px rgba(255, 107, 53, 0.4);
}

@keyframes fadeInUp {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}

.hook {
  font-size: 1.4rem;
  font-weight: 700;
  background: linear-gradient(135deg, var(--highlight), #fff8e1, #fff3cd);
  padding: 2.5rem;
  border-left: 8px solid var(--viral-red);
  margin: 2.5rem 0;
  line-height: 1.7;
  border-radius: 0 12px 12px 0;
  box-shadow: 0 10px 25px rgba(229, 57, 53, 0.2);
  position: relative;
  overflow: hidden;
  animation: highlight 2s infinite alternate;
  border: 2px solid #ffeaa7;
}

@keyframes highlight {
  from { box-shadow: 0 10px 25px rgba(229, 57, 53, 0.2); }
  to { box-shadow: 0 10px 35px rgba(229, 57, 53, 0.4); }
}

.content-section {
  background-color: var(--card-bg);
  padding: 3.5rem;
  border-radius: 16px;
  box-shadow: var(--card-shadow);
  margin-bottom: 3.5rem;
  border: 1px solid var(--border-color);
  position: relative;
  overflow: hidden;
  animation: fadeIn 0.8s ease-out;
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.content-section:hover {
  transform: translateY(-5px);
  box-shadow: 0 15px 40px rgba(0,0,0,0.15);
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(30px); }
  to { opacity: 1; transform: translateY(0); }
}

.content-section::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 10px;
  height: 100%;
  background: linear-gradient(to bottom, var(--viral-red), var(--trending-purple));
}

.thematic-analysis,
.storytelling-insights,
.production-breakdown,
.listener-takeaway {
  margin-bottom: 3rem;
  padding: 2.2rem;
  background: linear-gradient(135deg, #f8f9fa, #ffffff);
  border-radius: 12px;
  border-left: 6px solid var(--accent-color);
  position: relative;
  box-shadow: 0 6px 15px rgba(0,0,0,0.05);
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.thematic-analysis:hover,
.storytelling-insights:hover,
.production-breakdown:hover,
.listener-takeaway:hover {
  transform: translateX(5px);
  box-shadow: 0 10px 20px rgba(0,0,0,0.1);
}

.call-to-action {
  background: var(--button-gradient);
  color: white;
  padding: 3rem;
  border-radius: 16px;
  margin: 3rem 0;
  text-align: center;
  box-shadow: 0 12px 30px rgba(255, 107, 53, 0.4);
  animation: float 4s ease-in-out infinite;
  border: 3px solid rgba(255, 255, 255, 0.2);
}

@keyframes float {
  0% { transform: translateY(0px); box-shadow: 0 12px 30px rgba(255, 107, 53, 0.4); }
  50% { transform: translateY(-12px); box-shadow: 0 18px 40px rgba(255, 107, 53, 0.6); }
  100% { transform: translateY(0px); box-shadow: 0 12px 30px rgba(255, 107, 53, 0.4); }
}

.call-to-action h3 {
  color: white;
  font-size: 2rem;
  margin-top: 0;
  margin-bottom: 1.5rem;
  text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.3);
}

.call-to-action p {
  font-size: 1.3rem;
  margin-bottom: 2rem;
  max-width: 700px;
  margin-left: auto;
  margin-right: auto;
}

.btn {
  display: inline-block;
  background: linear-gradient(45deg, var(--accent-color), #0088b3);
  color: white;
  padding: 1.2rem 2.5rem;
  border-radius: 35px;
  text-decoration: none;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 1.2px;
  transition: all 0.4s ease;
  border: none;
  cursor: pointer;
  font-size: 1.15rem;
  box-shadow: 0 8px 15px rgba(0, 168, 204, 0.4);
  position: relative;
  overflow: hidden;
}

.btn:hover {
  transform: translateY(-4px);
  box-shadow: 0 12px 20px rgba(0, 168, 204, 0.6);
  background: linear-gradient(45deg, #0088b3, var(--accent-color));
  letter-spacing: 1.5px;
}

.btn:active {
  transform: translateY(-2px);
}

.actions-list {
  list-style-type: none;
  padding-left: 0;
}

.actions-list li {
  padding: 1.2rem 0;
  padding-left: 3rem;
  position: relative;
  border-bottom: 1px solid var(--border-color);
  font-weight: 600;
  font-size: 1.1rem;
  animation: slideInLeft 0.3s ease-out;
  transition: background-color 0.3s ease;
}

.actions-list li:hover {
  background-color: rgba(255, 107, 53, 0.05);
  padding-left: 3.5rem;
}

.actions-list li:nth-child(even) {
  background-color: #fafafa;
  border-radius: 8px;
  margin: 0.3rem 0;
  padding-left: 3.5rem;
}

.actions-list li:last-child {
  border-bottom: none;
}

.actions-list li::before {
  content: "🔥";
  position: absolute;
  left: 0;
  top: 50%;
  transform: translateY(-50%);
  font-size: 1.4rem;
}

.sources {
  background: linear-gradient(135deg, #f5f5f5, #ffffff);
  padding: 2.5rem;
  border-radius: 12px;
  border-left: 6px solid var(--trending-purple);
  margin-top: 3rem;
  box-shadow: 0 6px 15px rgba(0,0,0,0.05);
}

.sources ul {
  padding-left: 1.8rem;
}

.sources li {
  margin-bottom: 1.2rem;
  padding-left: 1.2rem;
  position: relative;
}

.sources li::before {
  content: "🔗";
  position: absolute;
  left: -1.8rem;
  top: 0;
}

.sources a {
  color: var(--trending-purple);
  text-decoration: none;
  font-weight: 600;
  position: relative;
  padding-bottom: 2px;
  border-bottom: 1px dotted var(--trending-purple);
  transition: all 0.3s ease;
}

.sources a:hover {
  color: var(--primary-color);
  border-bottom: 1px solid var(--primary-color);
  padding-bottom: 4px;
}

.related-posts {
  margin-top: 4rem;
  padding-top: 3rem;
  border-top: 4px solid var(--primary-color);
  background: linear-gradient(to bottom, transparent, #f8f9fa);
}

.post-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(340px, 1fr));
  gap: 3rem;
  margin-top: 3rem;
}

.post-card {
  background-color: var(--card-bg);
  border: 1px solid var(--border-color);
  border-radius: 16px;
  padding: 2.5rem;
  transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  box-shadow: var(--card-shadow);
  position: relative;
  overflow: hidden;
  animation: cardHover 0.3s ease-out;
  background: linear-gradient(135deg, #ffffff, #fdfdfd);
}

.post-card::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 6px;
  background: linear-gradient(to right, var(--viral-red), var(--primary-color));
}

.post-card:hover {
  transform: translateY(-15px) scale(1.02);
  box-shadow: 0 20px 45px rgba(0, 0, 0, 0.18);
  border-color: var(--primary-color);
}

@keyframes cardHover {
  from { transform: translateY(0); }
  to { transform: translateY(-5px); }
}

.post-card h3 {
  margin-top: 0;
  margin-bottom: 1.2rem;
}

.post-card h3 a {
  color: var(--primary-color);
  text-decoration: none;
  font-weight: 700;
  font-size: 1.4rem;
  display: block;
  transition: color 0.3s ease, text-shadow 0.3s ease;
  position: relative;
  padding-bottom: 0.5rem;
}

.post-card h3 a::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 0;
  width: 0;
  height: 2px;
  background: var(--secondary-color);
  transition: width 0.3s ease;
}

.post-card h3 a:hover {
  color: var(--secondary-color);
  text-shadow: 1px 1px 2px rgba(247, 147, 30, 0.2);
}

.post-card h3 a:hover::after {
  width: 100%;
}

.affiliate-disclaimer {
  background: linear-gradient(135deg, #fff8e1, #fff3e0, #ffecb3);
  padding: 2.5rem;
  border-radius: 12px;
  border-left: 6px solid var(--secondary-color);
  margin: 3rem 0;
  border: 2px solid #ffd54f;
  box-shadow: 0 8px 20px rgba(255, 213, 79, 0.3);
}

.product-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 2rem;
  margin-top: 2rem;
}

.product-card {
  background: white;
  border: 1px solid var(--border-color);
  border-radius: 12px;
  padding: 2rem;
  text-align: center;
  box-shadow: 0 6px 12px rgba(0,0,0,0.08);
  transition: all 0.3s ease;
  position: relative;
  overflow: hidden;
}

.product-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 4px;
  background: var(--button-gradient);
}

.product-card:hover {
  transform: translateY(-8px);
  box-shadow: 0 15px 30px rgba(0,0,0,0.15);
  border-color: var(--primary-color);
}

.product-card h4 {
  color: var(--primary-color);
  margin-bottom: 1rem;
  font-size: 1.2rem;
  transition: color 0.3s ease;
}

.product-card:hover h4 {
  color: var(--secondary-color);
}

.product-card .price {
  color: var(--success-green);
  font-weight: bold;
  margin: 1rem 0;
  font-size: 1.2rem;
  font-size: 1.3rem;
}

footer {
  background: linear-gradient(135deg, #2c3e50, #34495e, #4a637d);
  color: white;
  padding: 4rem 0;
  border-top: 5px solid var(--accent-color);
  margin-top: 5rem;
  box-shadow: inset 0 10px 20px rgba(0,0,0,0.2);
}

footer .container {
  text-align: center;
}

footer p {
  margin-bottom: 1.2rem;
  color: #bdc3c7;
  font-size: 1rem;
}

footer a {
  color: #85c1e9;
  text-decoration: none;
  font-weight: 600;
  padding: 0 0.5rem;
  transition: color 0.3s ease, text-shadow 0.3s ease;
  border-radius: 4px;
  padding: 0.2rem 0.5rem;
}

footer a:hover {
  color: #5dade2;
  text-shadow: 0 0 8px rgba(133, 193, 233, 0.5);
  background: rgba(133, 193, 233, 0.1);
}

/* Social sharing bar */
.social-share {
  display: flex;
  justify-content: center;
  gap: 1.5rem;
  margin: 3rem 0;
  flex-wrap: wrap;
  padding: 1.5rem;
  background: linear-gradient(to right, #f8f9fa, #e9ecef);
  border-radius: 12px;
  border: 1px solid var(--border-color);
}

.social-btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 60px;
  height: 60px;
  border-radius: 50%;
  color: white;
  font-weight: bold;
  text-decoration: none;
  transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  box-shadow: 0 6px 12px rgba(0,0,0,0.2);
  font-size: 1.2rem;
  border: 3px solid white;
}

.social-btn.facebook { background: #3b5998; }
.social-btn.twitter { background: #1da1f2; }
.social-btn.instagram { background: linear-gradient(45deg, #405de6, #5851db, #833ab4, #c13584, #e1306c, #fd1d1d); }
.social-btn.reddit { background: #ff4500; }
.social-btn.copy-link { background: #2ecc71; }

.social-btn:hover {
  transform: translateY(-6px) rotate(5deg);
  box-shadow: 0 12px 20px rgba(0,0,0,0.3);
}

/* Enhanced typography */
blockquote {
  font-style: italic;
  padding: 1.5rem 2rem;
  margin: 2rem 0;
  background: linear-gradient(to right, #f8f9fa, #e9ecef);
  border-left: 5px solid var(--accent-color);
  border-radius: 0 8px 8px 0;
  position: relative;
  font-size: 1.2rem;
  line-height: 1.8;
}

blockquote::before {
  content: """;
  font-size: 4rem;
  position: absolute;
  top: -20px;
  left: 10px;
  color: rgba(0, 168, 204, 0.2);
  font-family: Georgia, serif;
  line-height: 1;
}

/* Animation enhancements */
@keyframes shimmer {
  0% { background-position: -1000px 0; }
  100% { background-position: 1000px 0; }
}

.shimmer {
  background: linear-gradient(to right, #f6f7f8 0%, #edeef1 20%, #f6f7f8 40%, #f6f7f8 100%);
  background-size: 1000px 100%;
  animation: shimmer 2s infinite linear;
}

/* Responsive design */
@media (max-width: 1024px) {
  .post-grid {
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 2.5rem;
  }
  
  .content-section {
    padding: 2.8rem;
  }
  
  h1 {
    font-size: 2.3rem;
  }
}

@media (max-width: 768px) {
  header .container {
    padding: 0 15px;
  }
  
  header h1 {
    font-size: 2.2rem;
  }
  
  nav ul {
    flex-direction: column;
    align-items: center;
    gap: 0.6rem;
  }
  
  nav li a {
    width: 90%;
    text-align: center;
    margin: 0.3rem 0;
  }
  
  .viral-badge {
    margin: 0.8rem 0 0 0;
  }
  
  .content-section {
    padding: 2.2rem;
  }
  
  .hook {
    padding: 1.8rem;
    font-size: 1.2rem;
  }
  
  .post-grid {
    grid-template-columns: 1fr;
    gap: 2.5rem;
  }
  
  h1 {
    font-size: 2.1rem;
  }
  
  h2 {
    font-size: 1.6rem;
  }
  
  .call-to-action {
    padding: 2.2rem;
  }
  
  .btn {
    display: block;
    width: 100%;
    margin: 0.8rem 0;
    text-align: center;
  }
  
  .trending-bar {
    font-size: 0.95rem;
    padding: 0.8rem;
  }
  
  .social-share {
    gap: 1rem;
  }
  
  .social-btn {
    width: 50px;
    height: 50px;
    font-size: 1rem;
  }
}

@media (max-width: 480px) {
  header h1 {
    font-size: 1.8rem;
  }
  
  h1 {
    font-size: 1.8rem;
  }
  
  h2 {
    font-size: 1.4rem;
  }
  
  .container {
    padding: 0 15px;
  }
  
  .content-section {
    padding: 1.8rem;
  }
  
  .hook {
    padding: 1.5rem;
    font-size: 1.1rem;
  }
  
  .call-to-action {
    padding: 1.8rem;
  }
  
  .call-to-action h3 {
    font-size: 1.6rem;
  }
  
  .call-to-action p {
    font-size: 1.1rem;
  }
  
  .viral-badge {
    font-size: 0.85rem;
    padding: 0.3rem 0.8rem;
  }
  
  .category-tag {
    font-size: 0.8rem;
    padding: 0.4rem 0.9rem;
  }
}